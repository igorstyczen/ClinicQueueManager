<Page
    x:Class="ClinicQueueManager.Views.OfficesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Style="{StaticResource PageRoot}">

    <Grid Margin="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource SectionTitle}" Text="Gabinety" />
            <TextBlock Style="{StaticResource SectionSubtitle}" Text="Zarządzanie gabinetami przychodni." />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,12,0">
                <DataGrid x:Name="OfficesGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="Grid_SelectionChanged"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" />
                        <DataGridTextColumn Header="Numer" Binding="{Binding Number}" Width="100" />
                        <DataGridTextColumn Header="Piętro" Binding="{Binding Floor}" Width="80" />
                        <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource FormCard}">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardHeader}" Text="Formularz gabinetu" />
                    <TextBlock Style="{StaticResource FormLabel}" Text="Numer gabinetu" />
                    <TextBox x:Name="NumberBox" />
                    <TextBlock Style="{StaticResource FormLabel}" Text="Piętro" />
                    <TextBox x:Name="FloorBox" />
                    <TextBlock Style="{StaticResource FormLabel}" Text="Opis" />
                    <TextBox x:Name="DescriptionBox" Height="80" TextWrapping="Wrap" AcceptsReturn="True" />
                    <WrapPanel Margin="0,16,0,0">
                        <Button Style="{StaticResource BtnAdd}" Content="Dodaj" Click="Add_Click" />
                        <Button Style="{StaticResource BtnSave}" Content="Zapisz zmiany" Click="Save_Click" />
                        <Button Style="{StaticResource BtnDelete}" Content="Usuń" Click="Delete_Click" />
                        <Button Style="{StaticResource BtnClear}" Content="Wyczyść" Click="Clear_Click" />
                    </WrapPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
